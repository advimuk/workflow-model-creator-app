import React from "react";
import BasicFlow from "../BasicFlow";

const ModelCreator = ({ components }) => {
  console.log("Entity Created", components);

  return <BasicFlow />;
};

export default ModelCreator;
